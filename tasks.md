现在需要仿照 OpenMAIC 项目（仓库已 clone 到本地 `D:\Develop\Project\Github\OpenMAIC`，项目简要介绍见 `openmaic.md`），将其中适合当前项目的核心能力引入到现有系统中，并以“课堂预演 / 教学模拟”的形式对教师端开放。

注意：
- 现有项目技术栈是Vue+FastAPI，而opencmaic项目是基于Next.js的，不能直接引入，前端也需要用Vue重写React页面，后端能直接copy复用则优先copy过来复用，其次用python参考源代码重新实现，最次做适应性修改。
- openmaic项目原本是针对学生端的课堂学习助手，但是我们的项目是提供给教师的教学智能体助手，为避免跑题风险，我们不打算开放学生端，而是以“课堂预演”/“教学模拟”的名义呈现
- 需要附加的功能是，我们希望能允许用户自行上传PPT和教案，ai预演能够使用用户提供的PPT，当然如果用户没有上传PPT，则使用openmaic原ppt生成和播放流程。另外目前我们的系统已经实现了ppt生成和教案生成等核心功能，所以在ppt生成后可以引导用户到预演模块。
- 讲解模式，光标聚焦等是主核心模式，我们主要也是参考这部分，白板辅助画画和圆桌讨论，可以适应性修改或删除，这部分需要与我交流讨论
- 前端的聚焦、白板、同步等功能是前端的难点，你需要反复思考，以实现原有效果，我不太确定你是否能直接复用原来react的组件，你需要也与我沟通来确定最好的方案
- 预演是“时间轴驱动”还是“对话驱动”。如果要像 OpenMAIC 那样稳定播，建议底层是时间轴/动作序列，聊天只是生成动作的方式。这里参考openmaic的实现方式即可

以上如果你有不同的或更好的建议，及时与用户沟通以改进

### 项目阶段：

1. 第一阶段只负责把原项目的核心功能搬过来，只保留 讲解 + 聚焦/高亮 + 翻页 + 可选语音，先不要上白板和圆桌。也不需要做用户自主上传ppt，以及自上传的ppt解析、预演，只需要根据原项目输入主题就行。