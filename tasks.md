现在前端的课件管理部分是死的数据，需要对接到后端，样式不用变动，课件管理页展示当前用户的 PPT、教案、手动上传文件三类数据，后期可能会添加html，gif等

注意：

- 页面样式不改，只替换数据获取和卡片映射逻辑。
- 保留现有筛选、列表/网格切换交互。
- 定义统一前端展示模型，至少要有：
  id、sourceType(ppt/lesson_plan)、name、modifyDate、daysAgo、status、coverImage?。
- PPT 走现有 listProjects()，教案可参考 [LessonPlanSidebar.vue] 的 /api/v1/lesson-plan/list 调用方式。
- 合并后按 updated_at 倒序展示。
- 当前卡片字段不够真实。现有 UI 还在显示 subject / grade / size / favorited，但 PPT/教案现有列表接口并没有这些字段，必须明确怎么处理。
- 课件管理页暂不展示分页控件
- “教案类型”怎么映射。现在筛选只有 pdf/ppt/video/word，教案继续映射成 word
- “只看生成过的”怎么定义。PPT 是不是 page_count > 0 才算生成过？教案是不是 content 非空才算？这个最好先定，不然会把草稿/空记录也展示出来。—只要存入数据库了，能查出来就算生成过了
- 卡片点击后去哪。PPT 应该回到历史项目/预览，教案应该打开对应教案详情；现在课件管理页卡片本身还没有真正的跳转逻辑。
- 点击ppt卡片跳转的页面与点击ppt历史项目卡片跳转的页面保持一致，点击教案卡片跳转到与点击教案文档卡片的页面一致，注意要展开富文本编辑区
- 要允许用户删除课件，编辑课件文档的信息，允许改标题上传的文件还可以改标签、类型、备注
- redis缓存ppt封面，ppt生成的历史项目也要redis缓存ppt封面
- “添加课件”按钮会失真。当前它是本地上传并塞进假 store；如果页面只展示 AI 生成的 PPT 和教案，这个按钮要么隐藏，要么改成跳去“新建 PPT / 新建教案”。—添加课件改成文件上传接口，用户点击自己上传的文件卡片不会做跳转到对应的历史记录里去，给友好提示即可。上传的内容也进同一列表，并标注清楚
- 收藏逻辑现在也是假的，而且 [PersonalCenter.vue]也依赖同一个 coursewareStore。你一旦改 store，个人中心收藏区也会一起受影响。简易实现收藏逻辑，不做持久化
- 时间筛选现在依赖 daysAgo，真实接入后要从 updated_at 现场计算，注意时区和“今天/近一周”的边界。
- 封面图只有 PPT 有，教案没有。教案卡片要用默认底图，否则网格会视觉不一致。
- 数据量变大后，前端合并分页会越来越重；如果后面用户数据很多，最好切后端聚合分页。–现阶段先不做分页
- 没有真实文件大小时，是显示 `—`
- 端标题为空时，允许我用“未命名教案/未命名PPT”兜底
- AI 内容用 updated_at 没问题，但上传文件是按 uploaded_at 排序

以上如果你有不同的或更好的建议，及时与用户沟通以改进